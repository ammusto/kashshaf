// Search pagination constants
export const PAGE_SIZE = 250;
export const MAX_RESULTS = 5000;
export const EXPORT_MAX_RESULTS = 2000;

/** Variants are only computed for queries returning at most this many hits.
 *  Above the cap the Variants button stays visible but disabled so users see
 *  the feature exists. */
export const VARIANTS_MAX_HITS = 5000;

// UI constants
export const ROW_HEIGHT = 48;

// Sidebar dimensions
export const SIDEBAR_MIN_WIDTH = 400;
export const SIDEBAR_MAX_WIDTH = 700;
